import { Schema, Is, a, an } from "@bluelibs/validator-bundle";
import { GroupUpdateInput as BaseGroupUpdateInput } from "./GroupUpdate.input.base";

@Schema()
export class GroupUpdateInput extends BaseGroupUpdateInput {
  // You can extend the base here
}
